version: 0.2
phases:
  install:
    runtime-versions:
      php: 7.3
  pre_build:
    commands:
      - composer install
      - npm init
      - npm install -g sass
      - npm install gulp
      - gulp --version

  build:
    commands:
      - gulp --version      
      - gulp build
  post_build:
    commands:
